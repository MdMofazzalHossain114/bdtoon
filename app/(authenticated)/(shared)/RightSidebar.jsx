import { auth } from "@/auth";
import { Avatar, AvatarFallback, AvatarImage } from "@/components/ui/avatar";

const RightSidebar = () => {
  return (
    <nav className="hidden lg:block w-full lg:w-1/5 bg-red-500">
      <h1>Name</h1>{" "}
    </nav>
  );
};

export default RightSidebar;
